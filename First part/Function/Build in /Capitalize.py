Name = input("Enter you name : ")

cp= Name.capitalize()
print(cp)


# 🧠 Key Points:
        # Only the first letter becomes capital
        # All other letters become small (lowercase)
        # It does not change the original string (strings are immutable)